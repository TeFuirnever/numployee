<!--
  用途：数字员工的环境事实台账——只记录"这台机器/这个环境里有什么"。
  何时改：MCP 服务增删、路径变化、账号环境变化时更新本文件。
  注意：本文件只放环境事实与配置链，不放策略——策略与纪律放 AGENTS.md。
  私有性：本文件按环境私有，建议不进版本库（加入 .gitignore），避免泄露内部拓扑。
  版本建议：按语义化版本管理——新增内容升 minor，删除/重命名条目或改变红线语义升 major，措辞修正升 patch。
  填写说明：所有「填写：」处替换为你自己的内容；示例仅为格式参考，填写后删除示例。
-->

# TOOLS.md — 环境事实台账

只记录环境事实与配置链路：有哪些服务、路径在哪、账号环境如何。
不放行为策略；策略请写 AGENTS.md，性格请写 SOUL.md。

## 本地环境事实表

### MCP 服务

| 服务名 | 传输方式 | 用途 | 备注 |
| --- | --- | --- | --- |
| 示例：search | stdio | 文档检索 | 仅内网可用 |
| 填写： |  |  |  |
| 填写： |  |  |  |

### 路径

| 名称 | 路径 | 说明 |
| --- | --- | --- |
| 示例：项目根 | /path/to/workspace | 员工的主工作区 |
| 示例：日志目录 | /path/to/logs | 只读 |
| 填写： |  |  |
| 填写： |  |  |

### 账号环境

| 项目 | 值/说明 |
| --- | --- |
| 示例：网络环境 | 公司内网，外网需代理 |
| 示例：凭据注入方式 | 环境变量注入，正文不出现明文 |
| 填写： |  |
| 填写： |  |

## 注意事项

- 本文件按环境私有，**建议不进入版本库**（加入 `.gitignore`），避免内部拓扑与账号细节外泄。
- 如需共享配置结构，可提交一份 `TOOLS.example.md` 脱敏副本。
- 可见的工具目录是运行时权威；本表帮助诊断"某工具为什么不存在"，但不授予任何可用性。
- 表格中不要记录明文密钥、令牌或密码；只记录注入方式与存放位置。
